{
  "description": "Vi IMproved text editor",
  "arch": [
    "sudo pacman -Syu --noconfirm vim"
  ],
  "ubuntu": [
    "sudo apt update",
    "sudo apt install -y vim"
  ],
  "debian": [
    "sudo apt update",
    "sudo apt install -y vim"
  ],
  "raspberrypi": [
    "sudo apt update",
    "sudo apt install -y vim"
  ]
}
