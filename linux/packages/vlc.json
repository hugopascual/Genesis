{
  "description": "VLC media player",
  "arch": [
    "sudo pacman -Syu --noconfirm vlc"
  ],
  "ubuntu": [
    "sudo apt update",
    "sudo apt install -y vlc"
  ],
  "debian": [
    "sudo apt update",
    "sudo apt install -y vlc"
  ],
  "raspberrypi": [
    "sudo apt update",
    "sudo apt install -y vlc"
  ]
}
