{
  "description": "Network downloader",
  "arch": [
    "sudo pacman -Syu --noconfirm wget"
  ],
  "ubuntu": [
    "sudo apt update",
    "sudo apt install -y wget"
  ],
  "debian": [
    "sudo apt update",
    "sudo apt install -y wget"
  ],
  "raspberrypi": [
    "sudo apt update",
    "sudo apt install -y wget"
  ]
}
