{
  "description": "Java Development Kit",
  "arch": [
    "sudo pacman -Syu --noconfirm jdk-openjdk"
  ],
  "ubuntu": [
    "sudo apt update",
    "sudo apt install -y default-jdk"
  ],
  "debian": [
    "sudo apt update",
    "sudo apt install -y default-jdk"
  ],
  "raspberrypi": [
    "sudo apt update",
    "sudo apt install -y default-jdk"
  ]
}
