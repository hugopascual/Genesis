{
  "description": "Transfer data with URLs",
  "arch": [
    "sudo pacman -Syu --noconfirm curl"
  ],
  "ubuntu": [
    "sudo apt install -y curl"
  ],
  "debian": [
    "sudo apt install -y curl"
  ],
  "raspberrypi": [
    "sudo apt install -y \\{package\\} curl"
  ]
}
