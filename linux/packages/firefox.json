{
  "description": "Mozilla Firefox web browser",
  "arch": [
    "sudo pacman -Syu --noconfirm firefox"
  ],
  "ubuntu": [
    "sudo apt update",
    "sudo apt install -y firefox"
  ],
  "debian": [
    "sudo apt update",
    "sudo apt install -y firefox"
  ],
  "raspberrypi": [
    "sudo apt update",
    "sudo apt install -y firefox"
  ]
}
