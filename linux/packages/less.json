{
  "description": "",
  "arch": [
    "sudo pacman -Syu --noconfirm less"
  ],
  "ubuntu": [
    "sudo apt update",
    "sudo apt install -y less"
  ],
  "debian": [
    "sudo apt update",
    "sudo apt install -y less"
  ],
  "raspberrypi": [
    "sudo apt update",
    "sudo apt install -y less"
  ]
}
