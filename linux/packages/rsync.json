{
  "description": "Fast file copying tool",
  "arch": [
    "sudo pacman -Syu --noconfirm rsync"
  ],
  "ubuntu": [
    "sudo apt update",
    "sudo apt install -y rsync"
  ],
  "debian": [
    "sudo apt update",
    "sudo apt install -y rsync"
  ],
  "raspberrypi": [
    "sudo apt update",
    "sudo apt install -y rsync"
  ]
}
