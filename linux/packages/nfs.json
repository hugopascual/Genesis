{
  "description": "NFS utilities",
  "arch": [
    "sudo pacman -Syu --noconfirm nfs-utils"
  ],
  "ubuntu": [
    "sudo apt update",
    "sudo apt install -y nfs-common"
  ],
  "debian": [
    "sudo apt update",
    "sudo apt install -y nfs-common"
  ],
  "raspberrypi": [
    "sudo apt update",
    "sudo apt install -y nfs-common"
  ]
}
