{
  "description": "Interactive process viewer",
  "arch": [
    "sudo pacman -Syu --noconfirm htop"
  ],
  "ubuntu": [
    "sudo apt update",
    "sudo apt install -y htop"
  ],
  "debian": [
    "sudo apt update",
    "sudo apt install -y htop"
  ],
  "raspberrypi": [
    "sudo apt update",
    "sudo apt install -y htop"
  ]
}
