# RaspberryPi OS Server specialized notes

## SSH access
The `ssh_server.sh` script is probably neede to execute the commands by hand in 
order to have access to the server previous to the project clonation.